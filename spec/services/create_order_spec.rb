require "rails_helper"

RSpec.describe "CreateOrder Service / SolidQueue" do
  it "enqueues a new order job" do
    pending "Implement CreateOrder service with SolidQueue"
  end

  it "processes the job and creates an order" do
    pending "Test async job execution"
  end
end
